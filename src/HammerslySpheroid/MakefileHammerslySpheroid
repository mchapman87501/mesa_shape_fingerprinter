#

INCS          =  .

CCPP	      =  /usr/bin/g++

CFLAGS = -O3 -D_FILE_OFFSET_BITS=64 -DHAVE_LARGEFILE_SUPPORT

LIB           = /usr/lib

RM            = rm -f

PROGRAM	      = HammerslySpheroid 

OBJS	      = HammerslySpheroid.o

SRCS	      = HammerslySpheroid.cpp

all:	$(PROGRAM)

HammerslySpheroid.o:	$(SRCS)
	$(CCPP) $(CFLAGS) -I$(INCS)  -c  $(SRCS) 


$(PROGRAM):     $(OBJS) 
	$(CCPP)  $(CFLAGS) $(OBJS)  -o $(PROGRAM) -I$(INCS) -lm


clean:;		$(RM) $(OBJS) $(PROGRAM) core 
