#include "testsvdunit.h"

int main (int /*argc*/, char const ** /*argv*/)
{
    // TODO:  Use unittest to identify the tests to run.
    bool result = testsvdunit_test();
    return result ? 0 : 1;
}
